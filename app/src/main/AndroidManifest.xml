<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.composify">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Composify">
        <activity
            android:name=".spotify.SpotifyActivity"
            android:label="@string/title_activity_spotify"
            android:theme="@style/Theme.Composify.NoActionBar"></activity>
        <activity
            android:name=".spotify.SpotifyDetailActivity"
            android:label="@string/title_activity_spotify_detail"
            android:theme="@style/Theme.Composify.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Composify.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".locoEarnings.ui.EarningsActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Composify.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>